node_modules/
dist/
.turbo/
# TypeScript's incremental build cache: rewritten by every `tsc -b`, so
# tracking it dirtied the tree on every build and conflicted on every
# merge of two branches that had both been built.
*.tsbuildinfo
*.log
.env
.env.*
!.env.example
.DS_Store
coverage/

# rendered email previews, from `pnpm --filter @bento/server preview:email`
.email-preview/
.native/
zig-out/

# design-sync: staged converter scripts, build output, machine state
.ds-sync/
ds-bundle/
.design-sync/.cache/
.design-sync/learnings/
.design-sync/node_modules

# generated declaration output for the design-system export
apps/web/types/
